# 服务器状态快照

> **用途**：给 Agent / 未来的自己快速恢复上下文。敏感密钥不进 Git；本文件只记 **主机名、路径、证书 ID、公网 URL** 等非秘密信息。  
> **最后更新**：2026-06-06（quiz-live 公网 HTTPS + WSS 端到端验证通过）

---

## 架构一览

```text
观众/控台/大屏  HTTPS
    https://sska.site/sfkdoc/quiz-live/*.html
              │
              ▼
    腾讯云 COS（大阪 publicity-1256190400）
    自定义源站域名 sska.site · 静态网站源站 · 前缀 /sfkdoc
              │
              │  wss://ws.sska.site  （quiz-live/data/ws-relay-host.txt）
              ▼
    腾讯云 Lighthouse 东京
    43.165.171.110 · ubuntu@VM-0-13-ubuntu
              │
    Nginx :443  TLS（ws.sska.site 证书）
              │  proxy_pass
              ▼
    pm2 quiz-relay · 127.0.0.1:8082 · /opt/quiz-live
```

---

## 公网 URL（当前）

基址：`https://sska.site/sfkdoc`（`PUBLIC_BASE_URL` 应与此一致）

| 端 | URL |
|----|-----|
| 主持控台 | https://sska.site/sfkdoc/quiz-live/admin.html?room=SFKMAIN |
| 观众答题 | https://sska.site/sfkdoc/quiz-live/answer.html?room=SFKMAIN |
| 现场大屏 | https://sska.site/sfkdoc/quiz-live/screen.html?room=SFKMAIN |
| 说明页 | https://sska.site/sfkdoc/quiz-live/index.html |
| WS（浏览器） | `wss://ws.sska.site/` |
| WS 健康检查 | `curl https://ws.sska.site/` → `quiz-live relay OK` |

默认房间码：`SFKMAIN`（`quiz-live/data/default-room.txt`）

---

## 腾讯云 COS（静态站）

| 项 | 值 |
|----|-----|
| 桶名 | `publicity-1256190400` |
| 地域 | `ap-osaka` |
| 桶内前缀 | `/sfkdoc`（本地仓库根目录 1:1 映射） |
| 自定义域名 | `sska.site` |
| 源站类型 | 静态网站源站 |
| CNAME 目标 | `publicity-1256190400.cos-website.ap-osaka.myqcloud.com` |
| HTTPS 证书 | 已绑定，SSL 证书 ID **`YDmj75H4`**（域名 `sska.site`, `www.sska.site`） |
| 证书到期 | 2026-09-04（免费 DV，约 90 天周期） |
| 本机部署命令 | `deploy\sync.bat`（读 `deploy/sync.env`，未入库） |

验证 HTTPS 是否生效：

```bash
openssl s_client -connect sska.site:443 -servername sska.site </dev/null 2>/dev/null | openssl x509 -noout -subject
# 期望含 sska.site，而非 *.cos.ap-osaka.myqcloud.com
```

---

## DNS（DNSPod）

NS：`megrez.dnspod.net`, `pink.dnspod.net`

| 主机记录 | 类型 | 指向 / 用途 |
|----------|------|-------------|
| `@`（或根） | CNAME | `publicity-1256190400.cos-website.ap-osaka.myqcloud.com` |
| `ws` | A | `43.165.171.110` |
| `_dnsauth` | TXT | `sska.site` 证书验证（已签发，可保留至续期说明要求删除） |
| `_dnsauth.ws` | TXT | `ws.sska.site` 证书验证（同上） |

---

## Lighthouse（WebSocket 中继）

| 项 | 值 |
|----|-----|
| 公网 IP | `43.165.171.110` |
| SSH 用户 | `ubuntu`（以 `deploy/quiz-live-relay.env` 为准） |
| 主机名 | `VM-0-13-ubuntu` |
| 应用目录 | `/opt/quiz-live` |
| 入口脚本 | `/opt/quiz-live/scripts/quiz-ws-relay.js` |
| pm2 进程名 | `quiz-relay` |
| 监听 | `0.0.0.0:8082`（HTTP 健康 + WebSocket upgrade） |
| 本机部署命令 | `deploy\quiz-live-relay.bat`（读 `deploy/quiz-live-relay.env`） |
| 首次装机脚本 | `deploy/quiz-live-server-setup.sh`（Node 20 + pm2） |

### Nginx（WSS）

| 项 | 值 |
|----|-----|
| 站点配置 | `/etc/nginx/sites-available/ws.sska.site` |
| 已启用链接 | `/etc/nginx/sites-enabled/ws.sska.site` |
| 证书文件 | `/etc/nginx/ssl/ws.sska.site.pem` |
| 私钥文件 | `/etc/nginx/ssl/ws.sska.site.key` |
| 443 | `proxy_pass http://127.0.0.1:8082` |
| 80 | 301 跳转 HTTPS |

SSL 证书 ID **`YDnZlgBX`**（仅 `ws.sska.site`），到期 2026-09-04。

**已知告警**（2026-06-06）：`nginx -t` 曾报 `conflicting server name "ws.sska.site"`，功能正常，建议日后 `grep -r ws.sska.site /etc/nginx/sites-enabled/` 去重。

**续期后必做**：SSL 控制台重新下载 Nginx 包 → 覆盖 `/etc/nginx/ssl/*` → `sudo systemctl reload nginx`（自动续费不会自动更新此目录）。

---

## SSL 证书清单（腾讯云）

| ID | 域名 | 部署位置 | 自动续费 |
|----|------|----------|----------|
| YDmj75H4 | `sska.site`, `www.sska.site` | COS 自定义域名 | 建议开启 |
| YDnZlgBX | `ws.sska.site` | Lighthouse Nginx 手动 | 建议开启（仍需手动部署到服务器） |

控制台「关联资源：未关联」对 **手动 Nginx 部署** 是正常的。

---

## 仓库内与公网相关的配置

| 文件 | 内容 / 作用 |
|------|-------------|
| `quiz-live/data/ws-relay-host.txt` | `ws.sska.site` |
| `quiz-live/data/default-room.txt` | `SFKMAIN` |
| `deploy/sync.env.example` | COS 同步模板 |
| `deploy/quiz-live-relay.env.example` | SSH 部署 relay 模板 |

---

## 验证清单（发版后）

1. `curl https://ws.sska.site/` → `quiz-live relay OK`
2. `openssl ... sska.site:443` → Subject 为 `sska.site`
3. 观众页 https 打开 → 顶部 **已连接**
4. F12 → Network → WS → 101（可选）
5. 控台 `admin.html` 能看到观众上线

---

## 待 Agent 注意

- **`deploy/部署脚本说明.md`** 中 WebSocket 小节仍写「8082 直连 / ws://IP」为旧描述；**生产环境以本文 WSS 架构为准**。
- favicon：quiz-live 四页已加 `../assets/favicon.svg`；需 **`deploy\sync.bat`** 同步到 COS 后线上才消除 `/favicon.ico` 403。
- 未入库文件：`deploy/sync.env`、`deploy/quiz-live-relay.env`、`deploy/cos.yaml`、Lighthouse SSH 私钥路径。

---

## 相关文档

- [`运维备忘-新手易忘事项.md`](运维备忘-新手易忘事项.md)
- [`部署脚本说明.md`](部署脚本说明.md)
- [`docs/guides/QUIZ_LIVE_GUIDE.md`](../docs/guides/QUIZ_LIVE_GUIDE.md)
