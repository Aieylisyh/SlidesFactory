# 复制本文件为 sync.env 并填入你的密钥（sync.env 已被 git ignore，勿提交）
#
# 控制台：腾讯云 → 对象存储 COS → 存储桶列表 → 复制名称与地域
# 密钥：访问管理 CAM → 子用户 → 访问密钥（建议仅授予目标桶读写）

# 子账号密钥（CAM → 用户 → 访问密钥）
# 403 时参考 deploy/cam-policy.sfkdoc.example.json
SECRET_ID=your-tencent-cam-secret-id
SECRET_KEY=your-tencent-cam-secret-key
BUCKET=publicity-1256190400
REGION=ap-osaka

# 上传到桶内路径：仓库根目录内容 1:1 映射到此前缀（不会多出一层 Project/ 或 sfkdoc/sfkdoc/）
# 例：本地 index.html → sfkdoc/index.html；本地 summerschool/ → sfkdoc/summerschool/
PREFIX=/sfkdoc

# 部署后对外访问前缀（sync 完成时会打印链接；末尾不要加 /）
# 默认 COS 域名示例：https://<桶名>.cos.<地域>.myqcloud.com/<PREFIX>
# 若已绑自定义域名或开启静态网站，填实际对外 URL
PUBLIC_BASE_URL=https://publicity-1256190400.cos.ap-osaka.myqcloud.com/sfkdoc

# 可选：coscli 可执行文件路径；留空则自动在 deploy/tools 或 PATH 中查找
# COSCLI_PATH=
